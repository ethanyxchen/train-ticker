{
  "name": "daily-schedule-cleanup-worker",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "gcp-build": "npm run build",
    "cleanup:daily-schedule": "node dist/retain-latest-schedules.js",
    "cleanup:daily-schedule:dev": "tsx src/retain-latest-schedules.ts",
    "start": "node dist/retain-latest-schedules.js --apply",
    "test": "tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
